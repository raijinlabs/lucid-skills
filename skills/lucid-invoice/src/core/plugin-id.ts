/** Canonical plugin identifier used across all Lucid Invoice components. */
export const PLUGIN_ID = 'lucid-invoice' as const;

/** Human-readable plugin name for display and logging. */
export const PLUGIN_NAME = 'Lucid Invoice' as const;

/** Plugin version — kept in sync with package.json at release time. */
export const PLUGIN_VERSION = '1.0.0' as const;
